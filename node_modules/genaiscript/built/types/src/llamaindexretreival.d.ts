import { Host, MarkdownTrace, ResponseStatus, RetreivalOptions, RetreivalSearchOptions, RetreivalSearchResponse, RetreivalService, RetreivalUpsertOptions } from "genaiscript-core";
export declare class LlamaIndexRetreivalService implements RetreivalService {
    readonly host: Host;
    private module;
    private READERS;
    constructor(host: Host);
    init(trace?: MarkdownTrace): Promise<void>;
    private getPersisDir;
    private createStorageContext;
    private createServiceContext;
    clear(options?: RetreivalOptions): Promise<{
        ok: boolean;
    }>;
    upsert(filenameOrUrl: string, options?: RetreivalUpsertOptions): Promise<ResponseStatus>;
    search(text: string, options?: RetreivalSearchOptions): Promise<RetreivalSearchResponse>;
    /**
     * Returns all embeddings
     * @returns
     */
    embeddings(options?: RetreivalOptions): Promise<RetreivalSearchResponse>;
}
//# sourceMappingURL=llamaindexretreival.d.ts.map