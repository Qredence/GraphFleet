import { Ora } from "ora";
import { ChatCompletionsProgressReport, GenerationProgress, Progress } from "genaiscript-core";
export declare class ProgressSpinner extends Progress implements GenerationProgress {
    readonly spinner: Ora;
    constructor(spinner: Ora);
    name: string;
    id: string;
    log(message: string): void;
    completion(value: ChatCompletionsProgressReport): void;
    report(value: {
        message?: string;
        count?: number;
        succeeded?: boolean;
    }): void;
    warn(message: string): void;
    stop(): void;
    get text(): string;
}
export declare function createProgressSpinner(message: string, interval?: number): ProgressSpinner;
export declare class ConsoleProgress extends Progress implements GenerationProgress {
    readonly options: ConsoleProgressOptions;
    constructor(options: ConsoleProgressOptions);
    name: string;
    id: string;
    count: number;
    log(message: string): void;
    completion(value: ChatCompletionsProgressReport): void;
    report(value: {
        message?: string;
        count?: number;
        succeeded?: boolean;
    }): void;
    warn(message: string): void;
}
export interface ConsoleProgressOptions {
    stream: boolean;
    quiet: boolean;
}
export declare function createConsoleProgress(options: ConsoleProgressOptions): ConsoleProgress;
//# sourceMappingURL=progress.d.ts.map