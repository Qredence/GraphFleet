import { TraceOptions } from "./trace";
export type CacheEntry<K, V> = {
    sha: string;
    key: K;
    val: V;
};
export declare class JSONLineCache<K, V> extends EventTarget {
    readonly name: string;
    private _entries;
    private constructor();
    static byName<K, V>(name: string): JSONLineCache<K, V>;
    private folder;
    private path;
    private initialize;
    keys(): Promise<K[]>;
    entries(): Promise<CacheEntry<K, V>[]>;
    getEntryBySha(sha: string): Promise<CacheEntry<K, V>>;
    get(key: K): Promise<V>;
    set(key: K, val: V, options?: TraceOptions): Promise<void>;
    getKeySHA(key: K): Promise<string>;
}
//# sourceMappingURL=cache.d.ts.map