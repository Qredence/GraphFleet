import { TraceOptions } from "./trace";
export declare function createFetch(options?: {
    retryOn?: number[];
    retries?: number;
    retryDelay?: number;
    maxDelay?: number;
} & TraceOptions): Promise<(input: string | Request | URL, init?: import("fetch-retry").RequestInitWithRetry<typeof fetch>) => ReturnType<typeof fetch>>;
export declare function statusToMessage(res?: {
    status?: number;
    statusText?: string;
}): string;
//# sourceMappingURL=fetch.d.ts.map