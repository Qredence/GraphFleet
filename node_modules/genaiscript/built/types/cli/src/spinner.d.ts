import { Ora } from "ora";
import { Progress } from "../../core/src/progress";
export declare class ProgressSpinner extends Progress {
    readonly spinner: Ora;
    constructor(spinner: Ora);
    report(value: {
        message?: string;
        count?: number;
        succeeded?: boolean;
    }): void;
    warn(message: string): void;
    stop(): void;
    get text(): string;
}
export declare function createProgressSpinner(message: string, interval?: number): ProgressSpinner;
//# sourceMappingURL=spinner.d.ts.map